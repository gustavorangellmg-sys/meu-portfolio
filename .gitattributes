# Auto detect text files and perform LF normalization
* text=auto

# Track large media/document files using Git LFS (Large File Storage)
*.mov filter=lfs diff=lfs merge=lfs -text
*.pdf filter=lfs diff=lfs merge=lfs -text
